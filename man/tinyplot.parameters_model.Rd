% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tinyplot.R
\name{tinyplot.parameters_model}
\alias{tinyplot.parameters_model}
\title{Forest plot of model parameters using \code{tinyplot}}
\usage{
\method{tinyplot}{parameters_model}(x, flip = TRUE, zero = TRUE, sort = FALSE, ...)
}
\arguments{
\item{x}{An object returned by \code{\link[=model_parameters]{model_parameters()}}.}

\item{flip}{Logical, if \code{TRUE} (default), coefficients are plotted
horizontally, with parameter names on the vertical axis.}

\item{zero}{Logical, if \code{TRUE} (default), a dashed reference line is drawn
at zero (resp. at one for exponentiated coefficients), and the estimate
axis is extended to include that value.}

\item{sort}{Logical, if \code{TRUE}, coefficients are sorted by size, largest
on top. If \code{FALSE} (default), parameters appear in model order.}

\item{...}{Other arguments passed to \code{\link[tinyplot:tinyplot]{tinyplot::tinyplot()}}, e.g. \code{theme}
or \code{palette}. User-supplied \code{xlim}, \code{ylim} or \code{ylab} override the defaults
set by this method. The \code{type}, \code{data}, \code{ymin} and \code{ymax} arguments are
fixed by this method and will be ignored.}
}
\value{
The input \code{x}, invisibly. The function is called for its side
effect of drawing a plot.
}
\description{
Draws a forest plot (coefficients as points, confidence
intervals as ranges) from a \code{parameters_model} object, using the
lightweight base-graphics package \code{tinyplot} instead of \code{ggplot2}. The
method is registered for \code{\link[tinyplot:tinyplot]{tinyplot::tinyplot()}} and its shorthand alias
\code{tinyplot::plt()}.
}
\details{
Random effects parameters are dropped from the plot, and for
models with a zero-inflation component, only the conditional (count)
component is shown. A message is printed in these cases.

This method requires \emph{tinyplot} version 0.7.0 or later, which supports
extending axis limits to cover the reference value.
}
\examples{
\dontshow{if (insight::check_if_installed("tinyplot", minimum_version = "0.7.0", quietly = TRUE)) withAutoprint(\{ # examplesIf}
\donttest{
library(tinyplot)
data(mtcars)

model <- lm(mpg ~ wt + cyl + gear, data = mtcars)
result <- model_parameters(model)
plt(result)

# sorted by coefficient size, using a theme
plt(result, sort = TRUE, theme = "classic")

# exponentiated coefficients place the reference line at 1
model <- glm(am ~ wt + hp, data = mtcars, family = "binomial")
result <- model_parameters(model, exponentiate = TRUE)
plt(result)
}
\dontshow{\}) # examplesIf}
}
\seealso{
The \code{plot} method for \code{parameters_model} objects, provided by the
\emph{see} package, for \code{ggplot2} based plots.
}
